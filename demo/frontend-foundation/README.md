# Trace 动作系统隔离 Demo

运行（在本工作区根目录）：`npx vite demo/frontend-foundation --host 127.0.0.1 --port 52820`

仅内存样本，无 IPC、localStorage 或真实计划读写。继承正式 workspace.css 语义 token；Demo 覆盖样式不进入应用。展示五类动作、组件确认、任务行单槽 5 秒撤销、主题及原生 dialog 键盘焦点。

待用户确认：按钮密度、危险色、组件确认范围、撤销期限和动效。用户验收后将决策重新实现为正式组件，原型届时吸收或删除。
