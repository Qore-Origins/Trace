# 弹回根治（数据层）：children_order 自愈重建 + 出生即登记

**类型**：fix
**日期**：2026-09-08 00:33
**影响范围**：src/main/services/storage-service.ts、test/storage-service.spec.ts

## 变更描述

用户实测拖拽仍弹回。现场取证（`D:\Desktop\Plan-Test\.trace\plan-library.json`）：顶层两个子项（文件夹 `132` + 计划 `465545`），`children_order` 只登记了 `["465545"]`。根因：**`createPlan`/`createFolder` 从出生起就不写顺序载体**——乐观更新（9b82117）落位正确，但权威刷新时未登记项 rank=MAX 被名称序顶回原位。antd 时代拖拽不生效同此根因，只是无乐观更新时表现为「没动」而非「弹回」。

## 修改详情

- [storage-service.ts] `insertOrderEntry` 自愈重建：与真实兄弟列表（`listPlanDirs`）合并——已登记项保持既有顺序、缺失项名称序补尾、被拖项按 clamp 索引插入。**任何一次 move 即把该父级载体修复为全量**，存量残缺库（如用户当前库）下一次拖拽自动痊愈，无需迁移
- [storage-service.ts] `createPlan`/`createFolder` 补 `insertOrderEntry(parent, name, MAX)`：出生即登记，杜绝新残缺
- [storage-service.spec.ts] 新增 2 例：残缺现场复刻（手工写残缺 meta → move → 断言权威顺序与落点一致 + 载体全量化）、出生即登记（A/F/B 创建序断言）；107 测试全绿

## 影响分析

- 与渲染层修复（9b82117 乐观更新）互补：乐观负责「松手即落定」，自愈负责「权威刷新不顶回」。弹回链路上的两个断点均已闭合
- 失败语义不变：folder 父级仍跳过（无载体，名称序）；`removeOrderEntry` 不变
- 过程备注：本修复期间工作区曾被另一模型的会话/用户回滚并发触碰（createPlan 调用两次被覆盖），经用户确认并发者已离场后第三次补入；9e094ec（DragOverlay 方案）已被用户判定「改的不好」回滚（见前一提交）
- 验证：typecheck + 107 测试 + build 全绿；建议 dev 复测路径：顶层拖 `465545` 到 `132` 后松手（应落定不弹回）→ 重启确认顺序持久化 → 检查 meta `children_order` 已全量
