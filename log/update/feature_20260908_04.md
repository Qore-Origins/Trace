# 树动效集成（卡牌摞 D1-D8）+ 偏好设置 + I18n 中英多语言

**类型**：feature
**日期**：2026-09-08 21:10
**影响范围**：新增 src/renderer/src/i18n/（index + locales/zh-CN + locales/en-US）、src/renderer/src/stores/pref-store.ts；重写 PlanTreePanel.tsx；改动 workspace.css、tree-utils.ts、main.tsx、App/TopBar/StatusBar/cards/ContentArea/SearchOverlay/NameDialogModal/WindowControls/OnboardingView/WorkspaceView、tree/app/plan/search/ui 五 store；test/tree-utils.spec.ts；package.json（i18next@26 + react-i18next@17）

## 变更描述

demo 定稿的树动效（卡牌摞隐喻 D1-D8）正式集成进应用；设置弹窗新增「偏好设置」：界面语言（中文/English）与树发牌方向（首张先发=默认 / 末张先发）均持久化；引入 I18n 基建完成渲染层全量中英适配。

## 修改详情

- [i18n/index.ts + locales/zh-CN.ts + locales/en-US.ts]：i18next + react-i18next；en-US 以 `typeof zhCN` 形状约束（键缺失编译期报错）；初始语言读 localStorage 持久化值防首帧闪中文；插值参数承载动态文案
- [pref-store.ts]：zustand persist（localStorage 'trace-prefs'）；language（默认 zh-CN）切换即 i18n.changeLanguage；dealDirection（默认 top）
- [PlanTreePanel.tsx] 重写：antd 时代扁平渲染 → 递归 TreeGroup + 行槽（.slot > .slot-inner > .deal > .tree-row）；发牌/收牌按 dealDirection 偏好排波次（effStagger D8 压缩）；收拢=状态即时翻转 + closingPaths 延迟卸载 + 中途再点取消回弹（settled 钉住防重播）；折叠按钮改方框 +/- 旋转变号（D4，懒加载中暂代 Loading 图标）；D5 父行脉冲以 expanded 翻转触发；D6 连接线/D7 持牌暗示随槽位结构生效；dnd 拖拽 transform 与发牌动画分层（.deal 包装）互不覆盖
- [workspace.css]：动效时序 token（--stagger/--t-deal/--t-gather/--t-slot + 三曲线）+ D1-D8 全套样式 + prefers-reduced-motion 无障碍禁用
- [tree-utils.ts]：flattenTree（扁平渲染）退役；新增 effStagger；kindOf/optimisticMove 保留
- [main.tsx]：I18nextProvider 挂载；antd ConfigProvider locale 随偏好联动（zh_CN/en_US）
- [WorkspaceView.tsx]：设置弹窗顶部新增「偏好设置」区（语言 Radio + 发牌方向 Radio）；快捷键/版本等文案 i18n
- 全组件与 store：中文字符串 → t()/i18n.t()（组件用 useTranslation hook；store 非 React 环境直调 i18n.t）——TopBar 菜单全量、cards 五类组件卡、ContentArea、SearchOverlay、StatusBar、NameDialogModal、WindowControls、OnboardingView、App、ui-store 删除确认、app-store 切库确认、tree-store 导入导出报告与移动提示、plan-store 保存错误、search-store 检索失败
- [test/tree-utils.spec.ts]：flattenTree 组退役；新增 effStagger 组（≤10 基准 / >10 压缩封顶 ~320ms / 下限 6ms）；optimisticMove 组保留

## 影响分析

- 树渲染架构从扁平换递归组：拖拽（乐观落位）、懒加载、外部刷新链路均不受影响（数据层 childrenMap/loaded 未动）
- 语言切换即时生效（React 组件订阅 t 的组件自动重渲；store 内 i18n.t 在下次调用时取新语言）；antd 内置组件文案随 ConfigProvider 联动
- 包体：JS +~157kB（i18next + 双语料）；CSS +~4.9kB（动效）
- 已知取舍：品牌名「溯源 Trace」与引导页 slogan 视觉元素保持中文品牌呈现（brand.name 键已备未用于顶栏，将来如需英文品牌再启用）

## 相关问题

用户需求（2026-09-08）：①动效集成到应用，偏好设置保留发牌方向选择（默认首张先发）；②引入 I18n 做多语言，当前阶段适配中、英
