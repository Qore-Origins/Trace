# AI 视觉概念图提示词（主题「迹 · Trace Mark」）

> 用途：主界面/溯源面板/引导页的视觉概念参照（不是实现稿）。建议每条生成 2-4 张挑选。生成后可放 `Resource/concept/`（不入库或酌情入库）。

## 通用负面提示词（每条都带）

> 负面：text errors, watermark, photo, 3D render, cluttered, dark mode, neon, glassmorphism overload, gradient background

## 1. 主界面（工作台）

**中文：**
> 桌面软件界面设计概念图，明亮简约：左侧文件树面板（280px，浅蓝选中态、树连接线可见、根节点是一个深蓝色圆点标记"源头"），右侧白色纸面内容区排着几张圆角卡片（单选计划卡、任务列表卡、引用样式注释卡），其中一条任务有蓝色实心圆点和蓝色文字表示进行中，完成任务带删除线且半透明；顶部一条白色工具栏中央有搜索框，底部细状态栏。主色 #1677FF 渐变到 #0958D9 只用于选中条和进行中圆点，整体大量留白、干净克制，扁平现代 UI，中文界面，16:10。

**English:**
> Clean desktop productivity app UI concept, light theme: left file-tree panel (280px, visible tree connector lines, selected row highlighted soft blue with a 3px blue gradient bar, root node marked by a deep indigo dot as "origin"), right content area on white paper-like background with a few rounded cards (single-goal card, task list card, quote-style note card), one task shows a solid blue dot with blue text (in-progress), completed tasks struck through and 55% opacity; slim top toolbar with centered search field, thin status bar at bottom. Accent #1677FF gradient to #0958D9 used only on selection bars and active dots; generous whitespace, minimal, flat modern UI, Chinese interface text, 16:10.

## 2. 溯源搜索面板（回望瞬间）

**中文：**
> 桌面软件搜索浮层概念图：白底圆角浮层悬于轻遮罩上，顶部输入框内写着"周报"，下方结果分三组（计划/任务/注释）各带分组小标题，命中关键词高亮为浅蓝底色，每项下方有一行灰色路径文字表示来源；背景隐约可见文件树与内容卡片，其中一张任务卡片正被一圈蓝色光晕脉冲点亮（正在"回到源头"）。克制扁平风格，主色 #1677FF，大量留白，中文界面。

**English:**
> Desktop app search overlay concept: white rounded floating panel over a subtle dimmed workspace, query "周报" in the input, results grouped into 计划/任务/注释 sections with keyword highlights in soft blue, each hit showing a small grey breadcrumb path underneath; behind the overlay, the workspace is dimly visible and one task card is being pulsed with a blue glow ring (locating back to origin). Flat minimal style, accent #1677FF, generous whitespace, Chinese UI.

## 3. 首启引导页（空即是信任）

**中文：**
> 桌面应用首屏引导概念图，居中极简构图：上方一个深蓝色圆点（源头），下方产品名"溯源 Trace"与口号"计划有迹可循"，一段两行说明文字（数据以文件夹保存在本地），底部一个蓝渐变主按钮"选择计划库目录"；背景从纯白到极浅蓝的柔和渐变，无任何其他元素，克制、信任感、扁平，中文界面，3:2。

**English:**
> Desktop app onboarding screen concept, ultra-minimal centered composition: a deep indigo dot ("origin") on top, app name 溯源 Trace with slogan 计划有迹可循 below, two lines of explanatory copy (data stored as local folders), one blue-gradient primary button 选择计划库目录; background soft gradient from pure white to very light blue, nothing else on screen — calm, trustworthy, flat, Chinese text, 3:2.
