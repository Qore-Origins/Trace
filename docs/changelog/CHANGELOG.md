# 变更日志（CHANGELOG）

> package.json 为版本唯一真源；发布前更新本文件与 docs/lifecycle。
> 0.1-0.8 为按 git commit 里程碑回溯编目（历史区间以 commit 锚定，日期为阶段结束日）。

## [0.11.0] - 2026-09-10 — 公测版 Beta 3：代码高亮与交互打磨

- 代码块语法高亮：注释/自定义组件的 Markdown 代码块按语言着色（highlight.js lib/common ~35 种常用语言；自写配色对齐 Trace 纸面；未知/未声明语言回退纯文本；+4 单测）——计划项「markdown渲染高亮、代码块高亮」落地
- 删除过渡动画：计划/文件夹删除先平滑收合再真删除——与新建的发牌入场对仗；Delete 快捷键统一动画
- 拖拽排序形变根治：被拖卡不再被拉伸成目标卡形状（CDP 实证 scaleY 2.34→1.0）
- 重名提示补全：新建/重命名失败弹真实原因（同名计划或文件夹已存在），对话框保持开启供改名
- 依赖新增：highlight.js（首个渲染层高亮依赖；此前为零依赖自研渲染器）
- 测试基线：173/173
- BR-008 记录（远期）：「导出为」图片/PDF/.plan

## [0.10.1] - 2026-09-10 — 公测版 Beta 2 修订

- 树操作不再触发整树重载（`c541982`）：目录级增删改移登记内部写（抑制 chokidar 回声）；`fs-external-change` 只定向刷新变更层（VS Code 借鉴——操作局部生效，展开状态保持）
- 树侧边栏拖拽调宽（`db2dbfe`）：180–520 约束 + 跟手拖拽（DOM 直改不重渲染）+ 宽度持久化
- 注释渲染器扩展（`560dee3`）：HTML 注释剥除（防字面泄漏）/引用块/分隔线/表格（+7 单测）
- 测试基线：169/169

## [0.10.0] - 2026-09-10 — 公测版 Beta 2：日记深化

> SDD 批次（9991475..8755483；含终审修复波 77b9579/71dae4b）

- 日记深化（四位一体）：
  - 自动日计划：日记根 `Diary/` 幂等维护（只建不补）+ 今日页模板三件套（heading/mood/note）——diary-service 经 `diary:ensure/month/day` 三通道取数
  - 日历热力：DiaryView 月历视图（周一为始、分数热力格、今日高亮、无记录日可点、切月循环跨年）
  - 统计趋势：均分/打卡/记录数与趋势折线——仅计有 mood 的日，无记录天不按 0 计
  - 记录顺滑：点日期即预览 + 「在树中打开」回溯定位（复用 Sprint 3 定位动作）；顶栏「日记」导航 + 树内「日记」根特殊标签
  - 契约零变更（BR-007）；renderer 不供给路径，main 自解析根；demo 定稿参照 demo/diary-view-demo.html
- 修复：
  - 树操作语义——新建子项（hover 快捷钮/右键菜单）仅容器节点（文件夹/库根），计划无子项语义（`8755483`）
  - 日记视图交互底座提升 App 级（搜索/新建/设置恢复）；切根强制重挂；watch 回声抑制补全（mkdir addDir 预登记——防震树与外部变更误报）
  - `.single-due` 左缘 28px 对齐；mood_date 清空回退今天（`2c4fb6f`）
- 测试基线：154/154（+33：diary-service 13 / diary-view-utils 15 / watch 抑制与注入链 5）

## [0.9.0] - 2026-09-09 — 公测版（Beta）：禁止瞬切整顿 + 版本线确立

- 禁止瞬间切换全量扫描修复（`47685d8`，纯 CSS 零组件改动）：
  - 布局顶动类 → B 方案 overlay：任务行删除钮（demo 选型定稿）、卡片操作钮、树行快捷钮——hover 淡入+滑入，内容零顶动
  - 属性挑变类 → `--t-ui: 180ms` 交互基线：行 hover 底色/状态换色/勾号淡入/过期红字/心情色阶渐变/focus 底线
  - 显示切换类 → `@starting-style` 挂载过渡：逾期标 max-width 平滑展开、溯源浮层入场
  - prefers-reduced-motion 全覆盖（即显即用）
- 心情分数结构缩减无空跳（`9da8595` shrink：共同大结构滚动 100→099 + 延时移除前导列）
- 版本号确立：**v0.9.0 公测版**（发布前哨——剩余：发布前对齐小修、真机验收、`npm run build:win`；1.0.0 为正式版）

## [0.8.0] - 2026-09-09 — 心情分数轮带动画定稿 + 开源

- demo 选型（`2cb1ce5..d2b46c5`）：V1 轮带式定稿、V2 翻页式出局（含 inline-bar 不可变换根因修复 `7e55e98`）
- 移植进应用：mood-roll-machine 纯状态机 + MoodScoreRoll 执行层 + 设置弹窗「心情分数动画」开关（轮带式/无动画，pref 持久化）
- 开源：MIT LICENSE + README 许可段 + 历史泄密扫描干净确认

## [0.7.0] - 2026-09-09 — 组件扩展 + 截止日期

- 新组件：mood 今日心情（0-100 小数+色阶+日期）、heading 标题（字号滑杆）、custom 自定义（Markdown 双态+预设快照保存/删除）
- 截止日期两级：计划头部 + single_plan 组件卡（可选可清空、过期红色警示）
- 注释 Markdown 渲染双态（零依赖 note-md）；mood/heading/custom 入搜索索引
- 迁移工具 `npm run migrate:md`（幂等/备份/自检）：D:\Desktop\Plan 8 个 md 迁入

## [0.6.0] - 2026-09-08 — 树动效卡牌摞 + 中英双语

- 卡牌摞动效 D1-D8 定稿移植（demo 选型；偏好设置：发牌方向 首张/末张先发，`--stagger/--t-deal` 时序 token）
- 中英 i18n 全量适配 + 设置弹窗（Ctrl+,）
- 折叠按钮失灵/弹回等三批竞态根治（`c9302c9..bca514f` 区间）

## [0.5.0] - 2026-09-08 — 溯源搜索（Sprint 3）

- 内存扫索引（检索定案）、搜索浮层、回溯脉冲定位——MVP 三模块齐（`86971f4`）

## [0.4.0] - 2026-09-07 — 交互收官：菜单/拖拽/引导

- VS Code 式菜单栏（文件/编辑/查看/帮助）+ 全局快捷键；「文件→设置」
- 计划树拖拽重制：@dnd-kit 自渲染扁平树（弹回根治、分区防抖、排序定稿）；组件卡拖拽排序同款手感
- 空库引导（内容区空态 + "+"号数据驱动补全根节点）

## [0.3.0] - 2026-09-07 — 计划库导入导出

- .plan 导入导出（fflate zip 复用存储契约）与 Markdown 旧计划迁入（`98cee50`）
- 新建文件夹（纯容器）+ 计划拖拽归类；界面整顿（入口收敛、锁页面滚动、控件统一）

## [0.2.0] - 2026-09-06 — Sprint 1-2 核心 + 无边框窗口

- 主进程：存储契约、原子写引擎、树缓存、IPC 白名单、chokidar 监视、61 单测（`1509b89`）
- 渲染器：主界面、计划树（懒加载+拖拽）、五类组件卡（渲染即编辑）、引导页、状态栏（`46bf4ea`）
- 无边框窗口：titleBarStyle hidden + 自绘窗口控制（三次定稿：极简裸图标）；状态栏切字根因修复（全局 reset）

## [0.1.0] - 2026-09-05 — 立项 + 工程骨架

- 生命周期文档 00/01、选型/架构（02 系统设计 + HLD + LLD 定稿）
- Spring 0 脚手架：Electron 44 + React 19 + antd 安全基线 + electron-builder 配置 + 应用图标接入
- Spike 1-4：检索定案内存扫、跨盘 EXDEV fallback、antd Tree 拖拽 PoC

---
