# 改动文档（2026-09-09 · 组件扩展 + 截止日期）

## 本次改动

### 新组件（插入菜单 +3）

| 组件 | 说明 |
|------|------|
| mood 今日心情 | 评分 0-100 可小数（≤2 位收敛，色阶冷→暖）+ 描述文本 + 日期（默认今天可改）|
| heading 标题 | 标题 + 字号滑杆 14-32px（默认 18），实时预览 |
| custom 自定义 | Markdown 双态卡（复用注释渲染器），可保存为预设（快照插入）、菜单/卡内保存与删除预设 |

### 截止日期（两级）

- 计划文档级：内容区计划头部「＋ 截止日期」→ 日期可改可清除，过期红色警示
- 计划组件级：single_plan 卡摘要下方同款日期控件（用户澄清：每一个计划组件都应有）

### 基础设施

- 搜索索引：mood/heading/custom 入索引（scope 复用现分组，SearchOverlay 零改动）
- 契约：`format_version: '1'` 不变；`PlanDocument.due_date?` / `SinglePlanPayload.due_date?` 均可选（旧文档零迁移）
- 测试：107 用例（+validation 3、+search 1、+scoreColor 3）

## 其他改动

- 工作区批次整理（用户拍板）：F1-F8 删除链修复、note 渲染双态、README/交接/规则文件、migrate-md 迁移工具等按主题提交（`5f35f9f..fa2e660` 区间）
- D:\Desktop\Plan 8 个 md 计划已迁入 Trace 结构（`npm run migrate:md` 可复用；备份/错误现场在桌面待用户确认后清理）

## 未覆盖 / 待续

- 预设重命名（第一版明确未做）；预设名重复允许
- 推荐后续组件（附件/目标进度/回顾/标签）未动——见设计文档 §八
